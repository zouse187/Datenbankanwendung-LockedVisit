Alle (10.06.2026):

- Folgende Dateien für die Website erstellt, damit man für den Anfang eine funktionierende Oberfläche hat, in der man sich mit der Datenbank verbinden kann:
	- assets/style.css
	- config/db.php
	- Controllers/db_login_check.php
	- public/db_login.php
	- public/db_logout.php
	- public/dashboard.php # Temporäre Datei

Manuel Borghardt (16.06.2026):

- Eine sequence erstellt für die Personen_id, Besucher_id und Angestellter_id, weil Oracle das nicht von selber tut (sequences.sql)
- Tabelle PERSON angepasst: Passwort hinzugefügt, für jeden Datensatz eine Mail eingefügt, Email auf not null gesetzt (Tabelle_PERSON_angepasst.sql)
- 2 Stored Procedures angelegt für die Anmeldung und für die Regestrierung (Stored_Procedure.sql)
- Folgende Dateien für die Website hinzugefügt:
	- public/user_login.php
	- public/user_logout.php
	- public/register.php
	- public/dashboard_besucher.php
	- public/dashboard_mitarbeiter.php
	- controllers/user_login_check.php
	- controllers/register_check.php

Joel Jantschik (01.07.2026 - 07.07.2026):

- Tabelle VALIDIERTE_BESUCHER erstellt, mit den Attributen BESUCHER_ID und GEFANGENER_ID. Um festzulegen, welcher Besucher welche Gefangenen besuchen kann, bzw. welche Gefangenen dem Besucher auf der Website angezeigt werden, nachdem er sich angemeldet hat. (Tabelle_VALIDIERTE_BESUCHER_erstellt.sql)
- Die Stored Procedure "GET_VALIDIERTE_GEFANGENE" angelegt, die alle Gefangenen, die für den Besucher validiert sind zurückgibt. In Controllers/get_gefangene.php werden dann alle Gefangenen in einer while-Schleife in eine Menge gepackt und in public/dashboard_besucher.php wird die Menge dann ausgelesen. (Stored_Procedures.sql)
- Datei Controllers/get_gefangene.php hinzugefügt
- Datei public/dashboard_besucher.php angepasst
- Datei assets/style.css angepasst

Joshua del Rosario Rijo (08.07.2026 - 10.07.2026):

- Tabelle BESUCHER_VALIDIERUNG erstellt, mit den Attributen BESUCHER_ID, GEFANGENER_ID und VALIDIERT. Dadurch kann ein Besucher für mehrere Gefangene validiert sein und man auf der Webseite als Besucher sehen kann für welche Gefangenen man validiert ist. (Tabelle_BESUCHER_VALIDIERUNG_erstellt.sql)
- 3 Stored Procedures angelegt für den Validierungsprozess vom Besucher (Stored Procedures_2). "GET_GEFANGENE_FUER_VALIDIERUNG" sucht alle Gefangenen die besuchbar sind heraus und zeigt dann für welche Gefangenen der Besucher noch nicht validiert ist. "GET_OFFENE_VALIDIERUNGEN" checkt einfach nur welche Validierungsanträge noch offen sind. "VALIDIERUNGSANTRAG_STELLEN" Prüft ob ein Validierungsantrag für einen Gefangenen gestellt wurde und gibt eine Rückmeldung aus.
- Datei Controllers/get_validierung_gefangene.php hinzugefügt
- Datei Controllers/get_offene_validierungen.php hinzugefügt
- Datei Controllers/validation_request.php hinzugefügt
- Datei public/dashboard_besucher.php angepasst
- Datei assets/style.css angepasst

Leonid Nikkel (11.07.2026):

-Tabelle GEFANGENEN_SLOTS erstellt, mit den Attributen SLOT_ID, GEFANGENER_ID, WOCHENTAG und UHRZEIT. Dadurch können individuelle und feste Besuchsfenster für jeden einzelnen Häftling in der Datenbank festgelegt werden. (Tabelle_GEFANGENEN_SLOTS_erstellt.sql)
-2 Stored Procedures für den Terminbuchungs- und Genehmigungsprozess angelegt (Stored_Procedures_3.sql). "TERMIN_BUCHEN" prüft die Verfügbarkeit anhand der Slots und speichert den Termin in der Tabelle BESUCHSZEITEN. "ANTRAG_ENTSCHEIDEN" verarbeitet das Approval-Workflow-Pattern für JVA-Mitarbeiter (Verschiebung genehmigter Anträge in VALIDIERTE_BESUCHER, Löschung abgelehnter Anträge).
-Bestehende Stored Procedures "GET_GEFANGENE_FUER_VALIDIERUNG" und "VALIDIERUNGSANTRAG_STELLEN" optimiert, um ID-Übersetzungen (PERSON_ID zu BESUCHER_ID) abzufangen und genaueres Filtern bereits gestellter Anträge zu gewährleisten.
-Fehlerbehandlung im PHP-Backend verbessert, um echte Oracle-Fehlermeldungen abzufangen und direkt im Dashboard anzuzeigen.
-Datei Controllers/termin_buchen.php hinzugefügt
-Datei Controllers/antrag_bearbeiten.php hinzugefügt
-Datei Controllers/get_offene_validierungen.php angepasst
-Datei Controllers/validation_request.php angepasst
-Datei public/dashboard_besucher.php umfassend überarbeitet: Layout in logische Bereiche gegliedert, TomSelect für eine Live-Gefangenensuche eingebaut und eine dynamische Terminvergabe implementiert, die über ein Dropdown-Menü nur die exakt verfügbaren, noch nicht belegten Besuchsfenster der nächsten 14 Tage anzeigt.
-Datei public/dashboard_mitarbeiter.php funktional erweitert, um eingehende Validierungsanträge anzuzeigen und diese über "Genehmigen" und "Ablehnen" Schaltflächen zu verarbeiten.

Manuel Borghardt (16.07.2026):

Die Website an sich hat zwar funktioniert. Jedoch war die Benennung einiger Proceduren und Tabellen echt schlecht gewählt, weshalb ich folgende Proceduren und Tabellen umbenannt habe:
	Proceduren:
	- GET_GEFANGENE_FUER_VALIDIERUNG -> GET_NICHT_VALIDIERTE_GEFANGENE
	- GET_OFFENE_VALIDIERUNGEN       -> GET_OFFENE_VALIDIERUNGSANTRAEGE

	Tabellen:
	- BESUCHER_VALIDIERUNG -> VALIDIERUNGSANTRAG

Und die beiden Tabellen BESUCHSZEITEN und GEFANGENER_SLOTS habe ich gelöscht, weil ich die Implementierung der Termin-Buchung geändert habe. Vorher wurden alle freien Termine in GEFANGENER_SLOTS gespeichert und alle belegten Termine in BESUCHSZEITEN. Das war nicht so ganz sinnig und die Namen waren total unpassend. Das Problem habe ich gelöts, indem ich eine Tabelle gemacht habe mit den Namen GEFANGENEN_TERMINE. Dort haben alle Termine die Spalte GEBUCHT, die entweder auf 1 oder 0 ist. Das heißt die Tabelle wird mit der Stored Procedure geupdatet, statt dass in zwei Tabellen die Termine drinne stehen.

Und ich habe die Procedure GET_GEFANGENEN_TERMINE und die Datei Controllers/get_termine.php hinzugefügt. Diese Procedure holt alle Termine für die Gefangenen, für die man validiert ist. Diese Termine werden dann ins drop down gepackt. Vorher wurde das mit SELECTS im frontend code (public-Ordner) gemacht. Jetzt wie gewünscht mit einer Procedure und im backend code (controllers-Ordner).


Joel Jantschik, Manuel Borghardt (19.07.2026)

Mitarbeiterprozess zum bearbeiten der Validereungsanträge hinzugefügt.
	Proceduren:
	- GET_OFFENE_ANTRAEGE
	- VALIDIERUNGSANTRAG_BEARBEITEN

Zudem haben wir eine neue Dateie hinzugefügt und zwei bearbeitet.
	Neu:
	- antrag_bearbeiten.php
	Darin werden die beiden neuen stored procedures ausgeführt.

	Bearbeitet:
	- style.css
	- dashboard_mitarbeiter.php
	In "dashboard[...].php" werden die Validerungsanträge in der Tabelle, auf der Website, hinzugefügt/ bzw. angezeigt.